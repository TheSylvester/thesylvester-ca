# thesylvester.ca

Personal site of Sylvester Wong — AI software engineer.

Built with Next.js (App Router, static export) and deployed to GitHub Pages.

- `pnpm dev` — local development
- `pnpm build` — static export to `out/`
